name: Deploy Vite app to GitHub Pages
on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build
      - name: Deploy 🚀
        uses: peaceiris/actions-gh-pages@v4
        with:
          publish_dir: ./build   # 👈 IMPORTANT
